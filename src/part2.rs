use iso_639_macros::language;

// The problem is their are dupe versions of countries. I know right, so
// 1. parse the tsv into two files bib and term
// 2. just do 1. in a proc macro
// 3. hard code it or something

// language!("../assets/iso-639-2.tsv");
